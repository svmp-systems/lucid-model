# Runtime: no third-party packages (stdlib only; LLM perception uses urllib).
# Editable install registers lucid, lucid-run, lucid-gen, and lucid-inspect.
-e .

# Dev / test (matches pyproject.toml [project.optional-dependencies].dev)
pytest>=8.0
ruff>=0.4

# Build (matches pyproject.toml [build-system])
hatchling
