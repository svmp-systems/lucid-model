Projector turns complex states into simpler and more clear representation which can be used by the model later on.
It's function is to change the internal signals and make them easier to interpret

reason: Embeddings are large and mixed up which might create problems upon interpetation. Projector cleans those signals which helps in decoding them better elevating the model's reasoning

projector should filter out only information which is relevant to the task
accepted compression threshold must be set along with threshold error

coding must be done as to balance robustness
