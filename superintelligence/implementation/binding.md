works by taking all active traces(global) and providing multiple candidate frames for context-op and interference to work on locally. 

it works by 
- setting frame seeds based on candidate regions and relations etc 
- attaching traces to these seeds 