“Given what perception segmented and what memory activated, what plausible event/relation/transform structures could explain this?”

works by taking all active traces(global) along with perception data and providing multiple candidate frames for context-op and interference to work on locally. 

it works by 
- use perception based hints to group and form candidate frame seeds
- attaching traces to these seeds 
- learned soft "spots" which are assigned based on affinity. 
- keep optionality by emitting multiple frames 